apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: reflectns
spec:
  selector:
    app: avyay-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
