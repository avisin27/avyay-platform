apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: reflectns
spec:
  selector:
    app: avyay-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
